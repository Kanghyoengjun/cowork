eclipse 다운로드
eclipse - download country - kakao - eclipse ide for java developers - next next next

JRE = Java Runtime Environment 
자바가상머신
module 체크해제
file name = public class name

메소드=함수
자바의 전역변수 = class 내부에 선언    class 외부 선언 불가
System.out.println    줄바꿈
System.out.print
System.out.printf     포맷("%d %s",a,b)

식별자(변수명)  _ , $  -> 쓸순 있으나 권장 X
첫번째문자 숫자 X      true,false,null X      예약어 X     % X
유니코드사용 -> 한글,일본어 가능

데이터타입 8개,              레퍼런스타입 1개
boolean     1bit            class
char        2byte           interface
byte        1byte           array
short       2byte           enum(열거)
int         4byte
long        8byte
float       4byte
double      8byte

*String 은 기본데이터타입이아니라 클래스임

리터럴
상수 - final
var - 타입생략하고 변수선언가능, 초기값지정해야함
자동타입변환         강제타입변환 - double d = 1.9;   int n = (int) d; -> 강제타입변환으로 0.9손실 -> n=1


public class Start {
	
	
	public static int sum(int a,int b) {
		return a+b;
	}
	
	public static void main(String[] args) {
	//	System.out.println("Start Java!!!");
		int i = 234;
		int s;
		char a;
		
		s = sum(i,20);
		a='?';
		
		System.out.println(a);
		System.out.println("Start Java");
		System.out.println(s);
	}
}
