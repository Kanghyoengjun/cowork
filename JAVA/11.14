상속이 아주중요하다

부모 - 자식
슈퍼 - 서브
유도 - 파생(derived class)

부모의 private 멤버는 자식이어도 접근불가

JAVA는 클래스 다중 상속(multiple inheritance) 불허
 C++은 다중 상속 가능
 C++은 다중 상속으로 멤버가 중복 생성되는 문제 있음
 자바는 인터페이스(interface)의 다중 상속 허용

모든 자바 클래스는 묵시적으로 Object클래스를 상속받음
 Java.lang.Object 클래스는 모든 클래스의 슈퍼 클래스

서브 클래스의 객체가 생성될 때, 생성자 실행??
- 서브와 슈퍼클래스의 생성자 둘다 실행된다. 슈퍼클래스의 생성자가 서브클래스보다 먼저 실행된다.
- 부모클래스의 생성자 실행이 먼저임, 호출은 반대로 자식부터 호출


***오늘 아주 중요한거 할거임!

업캐스팅(upcasting)
 서브 클래스의 레퍼런스를 슈퍼 클래스 레퍼런스에 대입
 슈퍼 클래스 레퍼런스로 서브 클래스 객체를 가리키게 되는 현상
